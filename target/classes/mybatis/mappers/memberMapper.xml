<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="memberMapper">
  	<!-- 회원 가입 -->
  	<insert id="memberInsert" parameterType="com.ruda.s3.model.board.MemberVO">
  		insert into member values(#{id}, #{pw}, #{name}, #{email}, #{phone}, 3)
  	</insert>
  	
  	<!-- 회원 탈퇴 -->
  	<delete id="noticeDelete" parameterType="java.lang.String">
  		delete member where id=#{id}
  	</delete>
 
 	<!--  로그인 select one -->
	<select id="memberSelect" parameterType="com.ruda.s3.model.board.MemberVO">
		select * from member where id=#{id} and pw=#{pw}
	</select>
	
	<!-- 회원정보 수정 업데이트  --> 
	<update id="memberUpdate" parameterType="com.ruda.s3.model.board.MemberVO">
		update member set pw=#{pw}, name=#{name}, phone=#{phone}, email=#{email} where id=#{id}
	</update>	
  
  </mapper>